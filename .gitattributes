# Normalise to LF in the repository; Git checks out with the platform's native endings.
# Without this the project churns CRLF/LF on every Windows commit.
* text=auto eol=lf

*.png binary
*.jpg binary
*.docx binary
*.DOCX binary
*.odt binary
